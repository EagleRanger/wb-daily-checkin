# v2.0.0

WorkBuddy 软件更新后，v1.0.0 使用的内部 API/JWT 路线已失效。v2.0.0 改用本地 WorkBuddy 界面坐标点击。

## 主要变化

- 新增四步界面点击执行器：头像、签到领积分、签到按钮、关闭弹窗。
- 新增本机鼠标点击校准，以 WorkBuddy 窗口宽高比例保存四个目标点。
- 新增 WorkBuddy 窗口查找、还原、置前和未运行时自动启动。
- 将首次本机校准、`--dry` 无点击试跑、用户在场真实试跑和定时任务分成四个阶段。
- 明确分辨率、Windows 显示缩放、主显示器、窗口大小和 WorkBuddy 版式变化时的重校准条件。
- 移除主分支中已失效的 API 脚本；v1.0.0 标签仍保留完整历史。

## 重要限制

- 相对坐标不是界面识别，不能直接跨机器复用配置。
- 执行时会移动鼠标并抢占前台，不适用于锁屏或无交互桌面。
- 退出码 `0` 只证明点击序列执行完毕，不证明 WorkBuddy 服务端已记录签到。

## 贡献

- EagleRanger：坐标点击方案、实机开发与需求验收。
- OpenAI Codex：Skill 规范化、迁移引导、风险边界、打包验证与发布协作。
